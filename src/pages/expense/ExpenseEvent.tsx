//component
import ProtectedLayout from '@layouts/protected'

const ExpenseEvent = () => {
  return (
    <ProtectedLayout>
      <div>ExpenseEvent</div>
    </ProtectedLayout>
  )
}

export default ExpenseEvent
